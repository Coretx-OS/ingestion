# OpenAI API Configuration
OPENAI_API_KEY=sk-proj-YOUR_API_KEY_HERE
LLM_MODEL=gpt-4o-mini

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Development: Allows http://localhost:3000 + ALL chrome-extension:// origins (auto-pattern matched)
# Production: Requires explicit extension ID from Chrome Web Store
# Format: chrome-extension://<extension-id>
ALLOWED_ORIGINS=http://localhost:3000

# Production example (uncomment and set your published extension ID):
# ALLOWED_ORIGINS=chrome-extension://YOUR_PUBLISHED_EXTENSION_ID_HERE

# Database Configuration
DATABASE_PATH=./data/secondbrain.db
