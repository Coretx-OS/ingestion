
Chrome Extension (MVP)
This folder will contain the minimal Chrome extension UI for Second Brain OS.

MVP screens
Capture (textarea + toggles)

Confirmation (filed)

Needs review (clarify + bucket buttons)

Fix (one-step correction)

Log (recent items)

API
The extension talks to the backend API described in backend/openapi.yaml.
